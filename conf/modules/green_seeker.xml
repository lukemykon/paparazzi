<!DOCTYPE module SYSTEM "module.dtd">

<module name="green_seeker">
  <doc>
    <description>
This module is an example module for the course AE4317 Autonomous Flight of Micro Air Vehicles at the TU Delft.
This module is used in combination with a color filter (cv_detect_color_object) and the navigation mode of the autopilot.
The avoidance strategy is to simply count the total number of orange pixels. When above a certain percentage threshold,
(given by color_count_frac) we assume that there is an obstacle and we turn.

The color filter settings are set using the cv_detect_color_object. This module can run multiple filters simultaneously
so you have to define which filter to use with the GREEN_SEEKER_VISUAL_DETECTION_ID setting.
    </description>
  </doc>
    <settings>
    <dl_settings>
      <dl_settings name="GreenSeeker">
         <dl_setting var="gs_color_count_frac" min="0" step="0.01" max="0.5"/>
      </dl_settings>
    </dl_settings>
  </settings>
  
  <dep>
    <depends>cv_detect_color_object</depends>
  </dep>
  <header>
    <file name="green_seeker.h"/>
  </header>
  <init fun="green_seeker_init()"/>
  <periodic fun="green_seeker_periodic()" freq="4"/>
  <makefile target="ap|nps">
    <file name="green_seeker.c"/>
  </makefile>
</module>
